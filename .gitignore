# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Environment
.env
.env.local
.env.*.local

# Node / React
node_modules/
build/
dist/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Runtime data
# Daily email subscriptions and send-dedupe state are local server data, never source.
data/
*.sqlite
*.sqlite3
*.db

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# User-owned archives live beside the repo and are never part of it. CI already
# refuses a tracked .zip; this stops `git add -A` from staging one in the first place.
*.zip
